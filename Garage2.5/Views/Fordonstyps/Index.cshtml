@model IEnumerable<Garage2._5.Models.Fordonstyp>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Typ)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Typ)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FordontypId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FordontypId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FordontypId })
        </td>
    </tr>
}

</table>
